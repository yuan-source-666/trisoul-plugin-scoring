# 记忆/技能插件评分基准（阶段 1）

适用对象：以"记忆层"或"技能/工具层"形态挂接在 AI agent 宿主（agent framework、IDE agent、聊天宿主、MCP 客户端等）上的插件、库或服务。基准刻意保持宿主无关：**不针对任何单一宿主（包括 trisoul_x/DSH）的实现细节定义分档**。

## 总则
- 10 个维度，每维 0–100 分，总分 1000。
- 每个分数必须挂证据：仓库文件/文档链接、issue/PR 记录、或可复现实验。查不到证据的维度记 **"信息不足"**，按该维度记 0 分入总分，但在表格中标注 ⚠，评述中说明"证据不足，非能力否定"。
- 无法从公开信息判断、必须部署实测的（检索命中率、延迟等），优先标注"信息不足，需实测"；仅在项目自带基准测试时按其自报数据给中间档分（≤70），并在评述中注明"自报数据"。
- 记忆类项目在维度 5（技能定义与调用协议）常无对应能力：记 N/A，**该维度按 50 分折算计入总分**（中性分），并在评述注明；技能类项目在维度 1–2 若完全无记忆能力，同样处理。这种"缺维度中性折算"避免惩罚单一形态项目，也避免两边各算一次。

---

## D1 记忆写入与提取机制（100 分）
**评什么**：记忆如何产生——自动从对话中提取还是手动写入；提取的判据（实质性判断、去重、冲突消解）；写入是否可控、可撤销。

| 分档 | 可观测特征 |
|---|---|
| 0 | 无写入机制（纯手动配置文件）或无法确认 |
| 40 | 仅手动写入/简单 API append，无提取、无去重 |
| 70 | 有自动提取管线（LLM 提取或规则判据），有基础去重 |
| 90 | 70 档 + 冲突消解/更新策略、可撤销/可编辑、写入策略可配置 |
| 100 | 90 档 + 经验证的提取质量评估（论文/基准/可复现实验） |

**证据要求**：提取代码路径或文档章节；去重/冲突逻辑的代码或说明；质量评估报告（如有）。
**信息不足情形**：闭源 SaaS 无文档说明提取机制 → 信息不足。

## D2 检索质量（100 分）
**评什么**：召回手段（向量/关键词/图/混合）、排序与 rerank、检索评测（LoCoMo/LongMemEval 等基准或自建评测）、注入策略（top-k 控制、相关性阈值）。

| 分档 | 可观测特征 |
|---|---|
| 0 | 无检索（全量注入或随机） |
| 40 | 单一检索手段（纯向量或纯关键词），无 rerank、无评测 |
| 70 | 混合检索（向量+词法/图）或带 rerank；有注入策略 |
| 90 | 70 档 + 公开评测数字（标准基准，注明数据集） |
| 100 | 90 档 + 评测可复现（脚本/数据公开）或第三方独立验证 |

**证据要求**：检索代码路径；基准结果（区分"自报"与"可复现"）。查无评测 → 上限 70。

## D3 作用域与隔离（100 分）
**评什么**：记忆/技能的作用域分层（会话/项目/用户/组织/全局）；多用户、多 agent、多租户隔离；命名空间与访问控制。

| 分档 | 可观测特征 |
|---|---|
| 0 | 单一全局存储，无任何隔离 |
| 40 | 仅会话级或仅用户级一种隔离 |
| 70 | ≥2 层作用域 + 显式命名空间（如按 app/user/agent 分库） |
| 90 | 70 档 + 多租户访问控制（权限/ACL）或跨 agent 记忆共享策略 |
| 100 | 90 档 + 企业级隔离文档化（RBAC/SSO/数据分区），或有隔离性测试 |

**证据要求**：作用域枚举的代码/schema；隔离参数文档；权限模型文档。

## D4 持久化与生命周期（100 分）
**评什么**：存储介质与可选后端；记忆的过期/衰减/淘汰；版本与迁移；崩溃安全；导入导出。

| 分档 | 可观测特征 |
|---|---|
| 0 | 无持久化（纯内存）或存储方式无法确认 |
| 40 | 单一后端（内存/文件/单库），无生命周期管理 |
| 70 | 可插拔后端 + 基础生命周期（更新、删除、至少一种淘汰机制） |
| 90 | 70 档 + 版本/血缘/迁移 + 崩溃安全写入（原子写/事务）+ 导入导出 |
| 100 | 90 档 + 多后端生产级支持（含托管服务）与升级迁移实测记录 |

**证据要求**：存储层代码；schema 迁移文件；淘汰/衰减逻辑代码。

## D5 技能定义与调用协议（100 分）
**评什么**：技能/工具如何声明（格式、schema、示例）、如何注册、如何被模型调用（协议：function calling / MCP / 自有 RPC）、参数校验与错误处理、权限审批。

| 分档 | 可观测特征 |
|---|---|
| 0 | 无技能/工具概念 |
| 40 | prompt 内嵌约定或硬编码工具，无标准 schema |
| 70 | 有结构化定义格式（JSON schema/YAML/装饰器）+ 注册 API + 调用协议 |
| 90 | 70 档 + 参数校验、错误回传规范、权限/审批开关、动态装载/热更新 |
| 100 | 90 档 + 采用或贡献开放标准（MCP 等），协议文档完备且多宿主可用 |

**证据要求**：技能定义示例文件；注册代码；协议文档；校验/审批代码。
**备注**：纯记忆库无技能维度 → N/A 记 50。

## D6 宿主兼容与集成成本（100 分）
**评什么**：支持的宿主/框架数量与深度；安装步骤数；配置面大小；API/SDK 完整度；与宿主生命周期钩子的贴合度。

| 分档 | 可观测特征 |
|---|---|
| 0 | 仅概念，无可安装产物 |
| 40 | 单一宿主，安装需手工改宿主代码 |
| 70 | 单一宿主正式安装路径（一条命令/标准包管理器）或 ≥2 宿主的官方集成 |
| 90 | 70 档 + ≥3 宿主/语言官方集成 + 独立 REST/MCP API（宿主无关可用） |
| 100 | 90 档 + 官方插件市场/生态位（被第三方框架列为推荐集成） |

**证据要求**：安装文档；集成目录（quickstart 列表）；包管理器发布记录（npm/PyPI）。

## D7 可观测性与调试（100 分）
**评什么**：写入/召回/注入的追踪；用量与成本统计；调试接口（查/删/改单条记忆）；评测挂钩。

| 分档 | 可观测特征 |
|---|---|
| 0 | 无任何日志或内省手段 |
| 40 | 基础日志 |
| 70 | 结构化事件（写入/召回/注入可追踪）+ 管理接口（CRUD 单条记忆/技能） |
| 90 | 70 档 + UI/仪表板或 CLI 内省工具 + 用量统计 |
| 100 | 90 档 + OpenTelemetry 级追踪集成或公开评测挂钩（可挂自定义评估器） |

**证据要求**：日志/事件代码；管理 CLI/UI 截图或代码；遥测集成文档。

## D8 安全与隐私（100 分）
**评什么**：数据外流面（本地可运行 vs 强制云）；静态加密与传输安全；认证授权；PII 处理；供应链（依赖最小化、可审计）。

| 分档 | 可观测特征 |
|---|---|
| 0 | 无任何安全考量痕迹，或强制把用户数据送到无协议保障的第三方 |
| 40 | 有本地运行选项，但无文档化安全措施 |
| 70 | 数据流清晰文档化 + 本地/私有部署可行 + 最小权限默认 |
| 90 | 70 档 + 静态加密、认证授权、PII/脱敏处理任两项有实现 |
| 100 | 90 档 + 第三方安全审计/SOC2 类声明，或加密/权限全栈实现并有测试 |

**证据要求**：数据流文档；加密/认证代码；审计声明链接。
**注意**：SaaS 型项目天然有更大外流面，评的是"外流面是否被诚实文档化并被控制"，不是"是否上云"本身。

## D9 性能与规模上限（100 分）
**评什么**：检索延迟与写入延迟；记忆条目/向量规模上限；并发能力；资源占用；有无公开基准。

| 分档 | 可观测特征 |
|---|---|
| 0 | 无性能信息且架构明显无法扩展（如全量线性扫描且无索引，规模声明缺失） |
| 40 | 有索引或分页等基础扩展手段，无数字 |
| 70 | 架构清晰支撑万级条目（索引/分片/连接池），有定性说明 |
| 90 | 70 档 + 公开性能基准（自报，注明条件） |
| 100 | 90 档 + 生产规模实证（案例/压测报告/可复现脚本） |

**证据要求**：基准脚本或报告；索引/分片代码；生产案例。
**默认**：查无任何性能信息 → 信息不足或 ≤40。

## D10 可维护性与社区生态（100 分）
**评什么**：许可证；提交活跃度与最近发布；测试覆盖；文档质量；issue 响应；贡献者多样性；下游依赖生态。

| 分档 | 可观测特征 |
|---|---|
| 0 | 无许可证 + 无维护（>18 个月无提交） |
| 40 | 有许可证但单人维护、无测试 |
| 70 | 活跃维护（6 个月内有提交）+ 有测试与像样的 README/文档 |
| 90 | 70 档 + CI、贡献指南、多贡献者、issue 有响应、版本发布规律 |
| 100 | 90 档 + 广泛下游生态（被其他项目依赖/集成）+ 组织化维护 |

**证据要求**：LICENSE 文件；提交/发布时间线；测试目录；贡献者数量；依赖它的项目数。
**注意**：star 数只作参考，不作档位判据。

---

## 打分流程
1. 先读 README + LICENSE + 目录结构 → 判定"记忆类 / 技能类 / 两者兼有"（按实际暴露的接口，不只看自我描述）。
2. 对 D1–D10 逐维取证，能挂链接的挂链接；挂不了的记"信息不足 ⚠"。
3. N/A 维度按 50 折算；信息不足按 0 计入并标 ⚠。总分 = 10 维之和，**总分旁必须列出折算/0 分的具体维度**，否则总分无效。
4. 涉及实测的断言（检索质量、延迟）一律标注"需实测"。
